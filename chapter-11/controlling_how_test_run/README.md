# Comandos para cargo tess

      <!-- the tests won’t interfere with each other if they share state -->
      $ cargo test -- --test-threads=1
      
      cargo test --help
      cargo test -- --help

      $ cargo test -- --show-output

